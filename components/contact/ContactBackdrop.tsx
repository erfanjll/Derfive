"use client";

import { SpotlightBackdrop } from "@/components/ui/SpotlightBackdrop";

/** Backwards-compatible alias: the Contact page's atmospheric stage dressing. */
export function ContactBackdrop() {
  return <SpotlightBackdrop />;
}
